#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Estrutura de dados
struct log
{
    char nome[50];
    int idade;
    char telefone[15];
    char endereco[100]
};

// função para adicionar um novo registro
void adicionarRegistro(struct log *registros, int *quantidade);

int main()
{
    printf("%d", quantidade);
    return 0;
}